function Stack(/* optional */ capacity) {

  // Public API available
  let entity = {
    empty: () => null,
    capacity: () => null,
    peek: () => null,
    push: (record) => null,
    pop: () => null,
  };

  return entity;
}

module.exports = Stack;
